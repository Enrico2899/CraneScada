# Comunicazione col PLC Siemens (protocollo S7)
python-snap7>=1.3

# Backend web (serve la pagina HMI/SCADA e trasmette i dati via websocket)
fastapi>=0.110
uvicorn[standard]>=0.27

# Analisi dati storicizzati (fase futura: confronto traiettorie, ecc.)
pandas>=2.2
